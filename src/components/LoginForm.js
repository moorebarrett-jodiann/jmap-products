import { auth, googleProvider } from '../config/Firebase';


function LoginForm() {

  return (
    <div className='container'>
      <form>
        <button className='btn btn-outline-primary text-danger'>Hello</button>
      </form>
    </div>
  )
}

export default LoginForm